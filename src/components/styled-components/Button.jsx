import styled from "styled-components";

const Button = styled.button`
  margin-bottom: 10px;
  font-size: 0.7rem;
  border-radius: 5px;
  padding: 5px 8px;
  &:hover {
    background: #e60059;
  }
`;

export default Button;
